import "./globals.css";
import type { Metadata } from "next";
import { Toaster } from "react-hot-toast";
import { Playwrite_IN, Mulish } from "next/font/google";

const playwriteIndia = Playwrite_IN({
  variable: "--font-playwrite-india",
  weight: ["100", "200", "300", "400"],
});

const mulish = Mulish({
  variable: "--font-mulish",
  subsets: ["latin", "vietnamese"],
  style: ["normal", "italic"],
  weight: ["200", "300", "400", "500", "600", "700", "800", "900", "1000"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

const bodyClassName = `antialiased h-full bg-secondaryBgImage bg-no-repeat bg-center bg-cover bg-fixed`;

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="h-screen">
      <body className={`${playwriteIndia.variable} ${mulish.className} ${bodyClassName}`}>
        <Toaster />
        {children}
      </body>
    </html>
  );
}
